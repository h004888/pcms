server:
  port: 8098
spring:
  application:
    name: ecom-ops-service
  datasource:
    url: jdbc:mysql://localhost:3306/pcms_ecom_ops
    username: pcms_user
    password: pcms_pass
  jpa:
    hibernate:
      ddl-auto: update
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include: "*"
