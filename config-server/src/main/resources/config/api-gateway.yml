# API Gateway (port 8080) - served via /api-gateway/default endpoint
# (không có spring.config.import vì đây là file đang được serve về client)
server:
  port: 8080
spring:
  application:
    name: api-gateway
  profiles:
    active: default
  autoconfigure:
    exclude:
      - org.springframework.boot.jdbc.autoconfigure.DataSourceAutoConfiguration
      - org.springframework.boot.hibernate.autoconfigure.HibernateJpaAutoConfiguration
      - org.springframework.boot.data.jpa.autoconfigure.DataJpaRepositoriesAutoConfiguration

# JWT secret (must match user-service)
app:
  jwt:
    secret: "pcms-jwt-secret-key-must-be-at-least-32-bytes-long-for-hs256-signing-2026"
