# Error #144: POST /api/v1/payments/webhook -> 401

[INFO] **INFO** - Authentication

## Tom tat

| Field | Value |
|-------|-------|
| **Error #** | #144 |
| **Service** | `payment-service` |
| **Controller** | `PaymentsWebhookAliasController` |
| **HTTP Method** | `POST` |
| **URL** | `http://localhost:8080/api/v1/payments/webhook` |
| **HTTP Status** | **401** |
| **Response Time** | 0.146s |
| **Root Cause Category** | Authentication |
| **Severity** | INFO |

## Scene-by-Scene Reproduction

### Scene 1: Test Script gui request
-----------------------------------------
Test script tu dong goi endpoint qua API Gateway:

```bash
POST http://localhost:8080/api/v1/payments/webhook
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...(JWT)
Content-Type: application/json
{}
```

**Thoi gian response:** 0.146s
        |
        v

### Scene 2: API Gateway nhan va route
-----------------------------------------
Request den API Gateway (port 8080) -> Route theo Path predicate:

```
Route: payment-service (lb://payment-service)
Predicate: Path=/api/v1/payments
Filters: StripPrefix=2
```

-> Gateway xac dinh target service: **payment-service**
        |
        v

### Scene 3: Service xu ly request
-----------------------------------------
Service **payment-service** nhan request qua Feign/LB -> DispatcherServlet -> Controller mapping:

```java
// payment-service/PaymentsWebhookAliasController.java
POSTMapping("/api/v1/payments/webhook")
public ResponseEntity<...> handle(...) {
    // ... business logic
}
```
        |
        v

### Scene 4: ERROR - Loi xay ra
-----------------------------------------
Service gap exception khi xu ly:

```
_No log evidence found in service log_
```

**HTTP Status tra ve:** **401**
        |
        v

### Scene 5: Response tra ve Client
-----------------------------------------
API Gateway nhan response tu service -> forward ve test script:

```json
{
  "status": 401,
  "url": "http://localhost:8080/api/v1/payments/webhook",
  "elapsed": 0.146s
}
```

Test script ghi nhan ket qua vao `test-results.json`.

---

## TOM LAI
1. Test script goi **POST /api/v1/payments/webhook**
2. Request di qua API Gateway -> route den `payment-service`
3. Service xu ly -> gap exception `Unknown Exception`
4. Tra ve HTTP **401** trong 0.146s

## Root Cause Analysis

**Loai loi:** Authentication | **Muc do:** INFO

JWT token khong hop le hoac bi thieu. Co the do: token expired, wrong role, security filter chua permit.

## De xuat Fix

Cap token hop le voi dung role, hoac kiem tra SecurityConfig co permit endpoint.

## Artifacts
- **Test result:** `test-results.json`
- **Log file:** `logs/payment-service.log`
- **Service endpoint:** `payment-service/PaymentsWebhookAliasController.java`

---
*Generated by PCMS Test Investigation on 2026-06-23 12:53:38*
